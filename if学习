#define _CRT_SECURE_NO_WARNINGS 1
#include<stdio.h>
int main()
{
	int score = 0;
	scanf("%d/n", &score);
	if (score < 60)
		printf("不及格\n");
	else if (score >= 60 && score < 90)
		printf("及格\n");
	else if(score >= 90 && score <= 100)
		printf("优秀\n");
	return 0;
}
